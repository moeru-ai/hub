{
  "name": "@moeru-ai/characters",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "pnpm build:tsup && pnpm build:json",
    "build:json": "tsx src/build-json.ts",
    "build:tsup": "tsup"
  },
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json",
    "./dist/*.json": "./dist/*.json",
    "./*.json": "./dist/*.json"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@moeru-ai/character-card-helpers": "workspace:^",
    "character-card-utils": "^2.0.3",
    "defu": "^6.1.4"
  }
}
